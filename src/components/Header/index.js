import React from 'react';
import './styles.css';

//stateless component (usa uma const em vez de class)
const Header = () =>
    <header id="main-header" > Header da página </header>;

export default Header;